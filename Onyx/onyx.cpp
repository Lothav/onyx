#include <iostream>

/*
 * 1. Check remote for version matching, if versions don't match show warning to use Launcher (If internet connection)
 * 2. Check datafiles integrity against downloaded md5 references, in case of error launcher
 * 3. Spin up resource and SDL subsystems
 */
int main(int argc, char** argv) {
	std::cout << "test" << std::endl;
}